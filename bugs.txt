1. 在某些JDK上 NIO 和 AIO 使用 SSLEngine 解密比较大的回应体时 会出现 Tag Mismatch! 的异常
2. NIO 和 AIO 开启 Keep-Alive 在读取比较大的回应时会有莫名其妙地等待刚好一分钟才收到最后的31个byte 然后顺利完成, 关闭Keep-Alive 或者是 小回应没有这个问题
3. LinkedClock在并发的时候会出现定时线程重复启动的问题! 但是这些操作是加锁的.
